Goals for this branch:
  - Within the graph GUI there should be a button that prompts the user to export the graph as an SVG file (SVG is a vector image file)
  - matplotlib has an in-built function to help you. Use "plt.savefig('plot.svg', format='svg')
  - 'plot.svg' can be any file name. Ideally create a function to base it on the date or prompt the user to enter a name for the graph somehow.
  - This branch ensures that we can easily export our data and implement it in the wiki.
